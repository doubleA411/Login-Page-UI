import 'package:flutter/material.dart';


const TextStyle kSignInStyle = TextStyle(color: Colors.white, fontSize: 30.0);

const TextStyle kEmailTextStyle = TextStyle(color: Colors.white, fontSize: 15.0);

const BoxDecoration kLabelStyle = BoxDecoration(
                          borderRadius: BorderRadius.all(
                            Radius.circular(10.0),
                          ),
                          boxShadow: [
                            BoxShadow(
                              color: Colors.white,
                              offset: Offset(5.0, 10.0),
                              blurRadius: 0.0,
                              spreadRadius: 0.0,
                            ),
                          ],
                        );